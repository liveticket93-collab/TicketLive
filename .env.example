
# Entorno de Desarrollo
# Valores posibles: development, production, test
NODE_ENV=development

# Configuración de IA (ChatBot)
# El sistema soporta múltiples proveedores. Configura al menos uno:

# 1. Groq (Recomendado actualmente en el código)
# Obtén tu llave en: https://console.groq.com/keys
GROQ_API_KEY=tu_api_key_de_groq

# 2. Google Gemini (Usado como fallback o para detección de mock)
# Obtén tu llave en: https://aistudio.google.com/app/apikey
GOOGLE_GENERATIVE_AI_API_KEY=tu_api_key_de_gemini

# 3. OpenAI (Opcional)
# Obtén tu llave en: https://platform.openai.com/api-keys
OPENAI_API_KEY=tu_api_key_de_openai

# Modo Mock para Desarrollo
# Si se establece en true, el chatbot simulará respuestas sin gastar tokens.
# También se activa automáticamente si no hay una API Key de Google configurada en desarrollo.
USE_CHAT_MOCK=false

# URL del Backend API
# En desarrollo suele ser http://localhost:3001
NEXT_PUBLIC_API_URL=http://localhost:3001
